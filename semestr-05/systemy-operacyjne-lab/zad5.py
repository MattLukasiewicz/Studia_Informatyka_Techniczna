import os
import sys
D =  sys.argv[1]
K= sys.argv[2]
for root, dirs, files in os.walk(D):
	for name in files:
		os.path.join(root, name)
		if os.path.isfile(path):
			if os.path(getsize(path) == 0 and not os.acess(path, os.X_OK)
				os.remove(path) with open(P, 'a') as f:
					f.write(name +'\n')
